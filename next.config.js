/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'www.google.com', 'www.facebook.com', 'www.instagram.com'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
